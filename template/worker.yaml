machine:
  install:
    image: ${install_image}
  network:
    hostname: ${hostname}
    interfaces:
      - deviceSelector:
          physical: true
        dhcp: true
cluster:
  network:
    cni:
      name: none
  proxy:
    disabled: true