machine:
  install:
    image: ${install_image}
  kubelet:
    extraMounts:
      - destination: /etc/iscsi
        type: bind
        source: /var/etc/iscsi
        options:
          - bind
          - rshared
          - rw
  network:
    hostname: ${hostname}
    interfaces:
      - deviceSelector:
          physical: true
        dhcp: true
cluster:
  network:
    cni:
      name: none
  proxy:
    disabled: true